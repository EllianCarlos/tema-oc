#ifndef __FILE_PARSER
#define __FILE_PARSER

#include <bits/stdc++.h>

using namespace std;

class FileParser
{
public:
  static string readFile(string);
  static vector<string> readFileLines(string);
};

#endif // __FILE_PARSER